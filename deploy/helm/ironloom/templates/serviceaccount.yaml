{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ironloom.serviceAccountName" . }}
  labels:
    {{- include "ironloom.labels" . | nindent 4 }}
{{- end }}
