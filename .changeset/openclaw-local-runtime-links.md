---
'@vannadii/devplat-openclaw': patch
---

Update the local Docker runtime helper to print the gateway token, tokenized dashboard URL, tokenized chat URL, and WebSocket URL before container logs start, and refresh the bundled OpenClaw runtime dependency to 2026.5.18.
