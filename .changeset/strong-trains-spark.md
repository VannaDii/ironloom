---
---

Refresh the root Vite development dependency to `8.0.7`.

This change updates the repository's root `vite` dev dependency and lockfile to
pick up the `8.0.7` release without changing any publishable package version.
