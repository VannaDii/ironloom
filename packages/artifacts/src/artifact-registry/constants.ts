/**
 * Current schema version for artifact registry records.
 */
export const ARTIFACT_REGISTRY_VERSION = 1;

/**
 * Stable timestamp used for deterministic default registry entries.
 */
export const DEFAULT_ARTIFACT_REGISTRY_UPDATED_AT = '2026-04-30T00:00:00.000Z';
