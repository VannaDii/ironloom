# 迁移说明

运行时状态从 `.devplat` 移到 `.ironloom`。现有 `.devplat` 记录只有在操作员明确决策后才应归档或导入。

旧的 OpenClaw 运行时不是 Ironloom 的一部分。历史迁移引用可以保留在迁移计划中，但当前运行手册和工作流面向 Rust 监督运行时。

公共文档表面现在位于 `site/guide-docs` 下的 VitePress，并发布到 `https://ironloom.dev`。
